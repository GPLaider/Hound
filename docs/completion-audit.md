# Completion audit

Current verification snapshot for 2026-08-27 KST. Current-code evidence is separated from older runtime smoke records; temporary evidence under `.smoke/` is excluded from the package.

## Current machine evidence

| Check | Result |
| --- | --- |
| `python -m compileall -q src tests` | exit `0` |
| `python -m pytest -W error -q` | 162 collected, all passed, exit `0` |
| `python -m pip install -e . --no-deps` | editable `hounds 0.2.0` installed, exit `0` |
| Mandatory runtime dependencies | none in `pyproject.toml`; `pytest>=8` is test-only |
| `src/hounds` | 20 Python modules, 5,316 physical lines, 253,583 bytes |
| Largest modules | `process.py` 762, `engine.py` 604, `pack.py` 491 lines |
| Final wheel | 73,162 bytes; SHA-256 `60290df2933ff625be566c9ab9574dc05e49910e7a796aa4a2d41a9d8c1f8d7c` |
| Final sdist | 99,785 bytes; SHA-256 `7cfada915227e63456f53598b49f4f81df78711f557a7782ac05acdf3b4a6cb5` |
| Package metadata/content | Metadata 2.4, `License-Expression: MIT`, `LICENSE` and `NOTICE.md` present; no Hound state, smoke, reference, build, distribution, auth, or config path included |
| Isolated installation | 50 files, 645,610 bytes after `--version` and `doctor` generated bytecode |
| Isolated `hound --version`, `doctor` | both exit `0`; version `0.2.0`; Codex 0.149.0 and AgentFlow detected; credential-home residue `0` |
| User-wide Hound skill | source, embedded installer text, and installed file are byte-identical; frontmatter/name/no-placeholder checks passed. Bundled `quick_validate.py` could not run because system Python has no PyYAML |
| Windows termination source boundary | guarded direct-worker `System32\taskkill.exe /PID <pid> /T /F` only |
| Forbidden Windows console APIs under `src/` | no `CTRL_C_EVENT`, `CTRL_BREAK_EVENT`, or `GenerateConsoleCtrlEvent` |

The suite covers proposal-only verification, CLI exits, baseline/path gates, state transitions, failed-fingerprint refusal, repeated-review Pack behavior, exact worker cancellation, cross-process locking, Windows live-PID creation-time hydration, final-only Writer checkpoint recovery, resumed Verifier artifacts, asynchronous recovery cancellation, strict AgentFlow native completed-node schema validation, direct Pack culling/synthesis, isolated authentication and user-skill snapshot cleanup, guarded stale-home recovery, and terminal commit ordering.

## Current runtime evidence

- Real Codex Solo run `20260824-035223-df426218` completed with one workspace-writing Writer and one read-only Verifier. Machine verification passed, path issues were empty, the Verifier returned `accept`, terminal state was `DONE`, CLI exit was `0`, and only `answer.py` changed.
- Real AgentFlow run `6e6728ed4c074e3cbe27dea82aea2d46` completed two independent read-only Scouts. They reported the exact `alpha = 1` and `beta = 2` lines; `facts.txt` retained SHA-256 `0AC4C86B004E2FB7E7799999B7CFC0571DDA1B27BEA46A1ABD8C518189F676EF`.
- Self-host discovery/fix run `20260824-141347-5ec8034b` used AgentFlow runs `ba7f91434473461386c80bb114b0307f` and `afba20e5e88043e1b89ce54c56919c94`. It found and fixed a Windows recovery defect where an unhydrated process-table creation time could misclassify the original live worker as a reused PID. The host verification passed each edited round; the evidence-first three-round contract exhausted before final review, so a separate read-only acceptance run supplies the terminal verdict.
- Release audit run `20260826-235755-aabe1f86` passed its pytest and compile gates and used AgentFlow run `91e1b6ca6f334a0b98146b25b6254d99`. One native node was marked completed while its imported output was only a JSONL progress event. Hound now requires the same closed Scout schema used by direct workers and marks malformed completed-node output failed; the regression test passes.
- Read-only acceptance run `20260827-001518-d3f27f1c` passed both authoritative gates but stopped before final review because its Writer treated unavailable sandbox Python as a reason to remain `continue`. The common Writer prompt now says not to duplicate contract gates and defines `candidate_done` as the handoff to Hound verification; the focused prompt regression passes.
- Lyriq run `20260824-152648-a5ddf5a3` demonstrated the missing-skill failure: the Writer and all three AgentFlow Scouts could not see global `lyriq-porting-safety`; no device or source state changed, and the Pack was cancelled after `556.562` active seconds.
- Real skill-projection run `20260824-170605-7e66529c` then loaded `lyriq-porting-safety` through the isolated home, reported serial `ZY22J58799` and default experimental slot B, passed machine verification plus the independent read-only Verifier, and committed durable `DONE`. It performed no device, WSL, network, or target-file edit.
- Final Windows timeout evidence targeted worker PID `47464`, recorded PPID/owner PID `54288`, plus child `conhost.exe` PID `31652`; `taskkill_pid_tree` exited `0`. Explorer PID `58620` and Codex app-server PID `28472` survived.
- During the final isolated-wheel checks, global config SHA-256 stayed `3055569389458A9FF5D17486AF477094EEDB73A914DA7D69041FA422D6520B25`; Hound did not change its `2026-08-27 07:35:16 KST` last-write time. Isolated-home residue was `0`.
- A transitional pre-isolation smoke caused Codex to add `[projects.'c:\users\administrator\documents\chatgpt\hound\.smoke\real-solo-0.2.0-safe'] trust_level = "trusted"` to global `config.toml`. Hound did not remove it because repository policy forbids modifying that file. Later isolated runs preserved whichever config was current at their start.
- The 500-line module goal is soft. `process.py` and `engine.py` are explicit exceptions; safety checks were not removed to hit a cosmetic line target.

## Historical runtime evidence

Older direct-Pack and partial AgentFlow artifacts remain regression context only. The current suite and completed run `6e6728ed4c074e3cbe27dea82aea2d46` are authoritative for 0.2.0.
